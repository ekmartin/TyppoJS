'use strict';

var Phaser = require('Phaser')
  , Game   = function() {};

module.exports = Game;

Game.prototype = {
  create: function() {
    // Load everything needed for the preloader state.
  },

  update: function() {
  },
};
